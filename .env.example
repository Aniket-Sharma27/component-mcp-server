# Bearer token required on all /mcp requests (Authorization: Bearer <value>).
# Server refuses to start if this is unset.
API_KEY=changeme

# Port the HTTP server listens on. Defaults to 3000 if unset.
PORT=3000
